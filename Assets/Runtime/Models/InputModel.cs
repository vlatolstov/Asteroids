using Runtime.Abstract.MVP;
using UnityEngine;

namespace Runtime.Models
{
    public class InputModel : BaseModel
    {
        protected override void OnEventPublished(IData eventData)
        {
            // if (eventData is FireGunPressed s)
            // {
            //     Debug.Log("InputModel: Fire Pressed signal received");
            // }
            
            // Physics2D.OverlapBox(Vector2.zero, Vector2.one, 0f, );
            // TrailRenderer trailRenderer = null;
            // trailRenderer.
        }
    }
}